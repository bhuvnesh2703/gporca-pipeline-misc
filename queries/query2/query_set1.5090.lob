select a.COB_DATE, a.ACCOUNT, a.CCC_PRODUCT_LINE, a.VERTICAL_SYSTEM, a.WM_BANKING_PRODUCT_GROUP, a.WM_BANKING_PRODUCT_SUB_GROUP, CASE WHEN a.WM_BANKING_PRODUCT_SUB_GROUP LIKE '%AGN%' THEN 'Agency' WHEN a.WM_BANKING_PRODUCT_SUB_GROUP LIKE '%Credit%' THEN 'Credit' WHEN a.WM_BANKING_PRODUCT_SUB_GROUP LIKE '%Govt%' THEN 'Treasury' ELSE 'Other' END AS Group, sum(USD_NOTIONAL) AS USD_NOTIONAL, sum(USD_EXPOSURE) AS USD_EXPOSURE, sum(MV) AS MV, SUM(USD_PV01SPRD) AS USD_PV01SPRD, SUM(USD_PV01) AS USD_PV01 from ( select a.POSITION_ID, a.COB_DATE, a.ACCOUNT, a.CCC_PRODUCT_LINE, a.VERTICAL_SYSTEM, a.BOOK, a.PRODUCT_TYPE_CODE, a.PRODUCT_SUB_TYPE_CODE, a.WM_BANKING_PRODUCT_GROUP, a.WM_BANKING_PRODUCT_SUB_GROUP, a.BANKING_PRODUCT_DESCRIPTION, a.PRODUCT_TYPE_NAME, SUM(COALESCE((USD_NOTIONAL) :: numeric(15,5),0))/1000 as USD_NOTIONAL, SUM(COALESCE((USD_EXPOSURE) :: numeric(15,5), 0)) / 1000 AS USD_EXPOSURE, SUM(COALESCE((USD_PROCEED) :: numeric(15,5), 0)) / 1000 AS MV, SUM(COALESCE((USD_PV01SPRD) :: numeric(15,5),0))/1000 AS USD_PV01SPRD, SUM(COALESCE((USD_IR_KAPPA) :: numeric(15,5), 0)/10) AS USD_IR_KAPPA, SUM(COALESCE((USD_CONVX) :: numeric(15,5), 0)/1000) AS USD_CONVX, SUM(COALESCE((USD_IR_UNIFIED_PV01) :: numeric(15,5), 0)) AS USD_PV01 from cdwuser.u_dm_wm_position a WHERE A.COB_DATE in ( '2018-02-28' ) AND A.CCC_TAPS_COMPANY = '1633' AND (A.VAR_EXCL_FL<> 'Y' OR A.BOOK IN ('MSDPB3M','MSDPT3M')) AND (A.ccc_banking_trading = 'BANKING' OR (A.CCC_HIERARCHY_LEVEL2 IN ('WEALTH MANAGEMENT', 'GLOBAL WEALTH MANAGEMENT') or a.CCC_business_area = 'US BANKS-LIQUIDITY') AND A.PRODUCT_TYPE_CODE ='REPO' AND A.CCC_BUSINESS_AREA NOT IN ('NON CORE MARKETS','CORE MARKETS')) group by a.POSITION_ID, a.COB_DATE, a.ACCOUNT, a.CCC_PRODUCT_LINE, a.VERTICAL_SYSTEM, a.BOOK, a.PRODUCT_TYPE_CODE, a.PRODUCT_SUB_TYPE_CODE, a.WM_BANKING_PRODUCT_GROUP, a.WM_BANKING_PRODUCT_SUB_GROUP, a.BANKING_PRODUCT_DESCRIPTION, a.PRODUCT_TYPE_NAME ) A WHERE a.WM_BANKING_PRODUCT_GROUP IN('AFS') GROUP BY a.COB_DATE, a.ACCOUNT, a.CCC_PRODUCT_LINE, a.VERTICAL_SYSTEM, a.WM_BANKING_PRODUCT_GROUP, a.WM_BANKING_PRODUCT_SUB_GROUP, CASE WHEN a.WM_BANKING_PRODUCT_SUB_GROUP LIKE '%AGN%' THEN 'Agency' WHEN a.WM_BANKING_PRODUCT_SUB_GROUP LIKE '%Credit%' THEN 'Credit' WHEN a.WM_BANKING_PRODUCT_SUB_GROUP LIKE '%Govt%' THEN 'Treasury' ELSE 'Other' END